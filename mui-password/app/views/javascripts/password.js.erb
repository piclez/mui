$(document).keyup(function(event){
  if (event.keyCode == 119) {
    <% if mui_password? %>
      window.location = '<%= url(:mui_password_exit) %>';
    <% else %>
      windowOpen('<%= url(:mui_password_read) %>');
    <% end %>
  }
});
